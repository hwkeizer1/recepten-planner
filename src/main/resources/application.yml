### Spring DATA SOURCE Configurations
spring:
  datasource:
    url: jdbc:sqlite:recepten-planner.db
  jpa:
    database-platform: org.sqlite.hibernate.dialect.SQLiteDialect
    hibernate:
      ddl-auto: update
   
### Logging
logging:
  level:
    '[nl.recipes]':  DEBUG
